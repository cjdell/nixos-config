[package]
name = "gpu-panel"
version = "0.1.0"
edition = "2021"
description = "GPU telemetry + control web panel (native sysfs reads and writes, no daemon)"

[dependencies]

[profile.release]
opt-level = 3
lto = true
